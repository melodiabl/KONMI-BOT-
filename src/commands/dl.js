// src/commands/dl.js

async function handleVideo(ctx) {
  // Lógica para descargar videos
  return { text: 'Función de video aún no implementada.' };
}

export default {
  name: 'video',
  description: 'Descarga un video de YouTube.',
  category: 'media',
  handler: handleVideo,
};
